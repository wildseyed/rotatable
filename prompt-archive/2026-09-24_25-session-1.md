# Session 1 archive — 2026-09-24/25

Owner's prompts and key decisions, chronologically, condensed (not verbatim).
Kept for continuity across context compaction; current state always in
`notebook.md`, plan in `plan.md` + `plan/phase-N/`.

## Vision (owner braindump, 2026-09-24)
- Reactable emulator for Monome Norns ("rotatable").
- Multi-level hierarchy: table navigation on top; lower levels = place blocks
  from a menu, enter/configure blocks, move/rotate/connect objects.
- Table is round, bigger than the screen; XYZ nav: **E1=Z(zoom), E2=X, E3=Y**,
  CCW negative / CW positive. Vectorized display (no bitmaps). Faint dotted
  grid superimposed for motion perception; table edge = arc.
- Graphics (Lua) vs sound (SuperCollider) split per norns; replicate Reactable
  audio in SC. Research online beyond the manual; **no webcam needed** (objects
  are virtual). Noted the Reactable mobile apps existed.
- Working style: md plans with checkable todos; `plan-archive/` +
  `prompt-archive/`; small pieces, write to file often. Owner has a norns
  with SSH access (credentials in `.device/`).

## Decisions (2026-09-24, via AskUserQuestion)
1. E1 CW = zoom in. 2. v1 = core 8 types. 3. v1 panels = envelope + 2D first.
4. LINK target = cycle by proximity. 5. (superseded) removal = long-press K3.
6. Subtype = K1+E2 in ROTATE.

## Key-map revision (2026-09-25, owner)
K1 tap = norns system menu (not "back"). **K2 = BACK, K3 = action,
K1+K3 = shifted action, K1+K2 = remove.** ("K3 acts, K2 retreats, K1 modifies.")

## Owner interventions during build
- Grid dots: not outside the table circle.
- Asked how unused blocks are stored → place menu confirmed (not a tray).
- "Can't verify phase 2 without placing objects" → REPL-driven demos + screenshots.
- Felt left out during subagent phase; then: "proceed on your own, involve me
  only for sound opinions or uncertainty."
- Filter seemed inert → sine vs saw explanation (engine was fine).
- Suggested a **REST API** for interactive testing → `tools/api.py`.
- Asked how to configure sequencer/loop beyond rotate; what OUT is → led to
  building step editor + sample browser panels.
- **Patch slots feature**: nav outside table; long press = store, short press =
  recall, long press again = delete.
- Provided archive.org drum-machines-collection link for samples.
- Reported "missing 3 try restart" screen error → `load`/`Engine.load` collision.

## Final state (2026-09-25)
v1 complete: all 6 plan phases done. See `notebook.md` decisions log and
`plan/phase-5/integration.md` for v2 backlog. Device: norns @ .device/, runs
`rotatable` from `~/dust/code/rotatable`. Tools: `tools/deploy.py`,
`tools/api.py` (port 8787).
